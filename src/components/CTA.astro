---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = Astro.currentLocale || getLangFromUrl(Astro.url);
const t = useTranslations(lang as any);
---

<section class="py-32 bg-nexus-gray-soft dark:bg-slate-950 transition-colors duration-300" id="contact">
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
        <div
            class="relative bg-white dark:bg-slate-900 rounded-[3rem] p-12 md:p-24 overflow-hidden border border-gray-100 dark:border-slate-800 shadow-sm dark:shadow-none text-center transition-colors duration-300"
        >
            <!-- Decorative background -->
            <div class="absolute top-0 right-0 w-80 h-80 bg-nexus-blue/5 dark:bg-nexus-blue/10 blur-[100px] rounded-full">
            </div>
            <div
                class="absolute bottom-0 left-0 w-80 h-80 bg-nexus-purple/5 dark:bg-nexus-purple/10 blur-[100px] rounded-full"
            >
            </div>

            <div class="relative z-10">
                <h2 class="text-4xl md:text-6xl font-display font-bold text-slate-900 dark:text-white mb-8 transition-colors">
                    {t('cta.title')}
                </h2>
                <p
                    class="max-w-2xl mx-auto text-lg text-slate-500 dark:text-slate-400 font-light leading-relaxed mb-16 transition-colors"
                >
                    {t('cta.desc')}
                </p>

                <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
                    <button
                        id="openContactBtn"
                        class="w-full sm:w-auto px-12 py-6 bg-slate-900 dark:bg-white text-white dark:text-slate-900 rounded-full font-bold text-sm uppercase tracking-[0.2em] hover:bg-slate-800 dark:hover:bg-slate-200 transition-all shadow-xl shadow-slate-200 dark:shadow-none hover:-translate-y-1"
                    >
                        {t('cta.btn1')}
                    </button>
                    <button
                        id="openContactBtn2"
                        class="w-full sm:w-auto px-12 py-6 bg-white dark:bg-transparent border border-slate-200 dark:border-slate-600 text-slate-900 dark:text-white rounded-full font-bold text-sm uppercase tracking-[0.2em] hover:border-slate-400 dark:hover:border-slate-400 transition-all"
                    >
                        {t('cta.btn2')}
                    </button>
                </div>

                <div
                    class="mt-16 flex flex-wrap justify-center gap-8 text-[0.7rem] font-bold uppercase tracking-widest text-slate-400 dark:text-slate-500 transition-colors"
                >
                    <span class="flex items-center gap-2"
                        ><span class="material-symbols-outlined text-sm">verified</span> {t('cta.feature1')}</span
                    >
                    <span class="flex items-center gap-2"
                        ><span class="material-symbols-outlined text-sm">schedule</span> {t('cta.feature2')}</span
                    >
                    <span class="flex items-center gap-2"
                        ><span class="material-symbols-outlined text-sm">rocket_launch</span> {t('cta.feature3')}</span
                    >
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    // Initialize event listeners
    document.addEventListener("DOMContentLoaded", () => {
        const btn1 = document.getElementById("openContactBtn");
        const btn2 = document.getElementById("openContactBtn2");

        if (btn1) {
            btn1.addEventListener("click", () => {
                window.dispatchEvent(new CustomEvent("openContactModal"));
            });
        }

        if (btn2) {
            btn2.addEventListener("click", () => {
                window.dispatchEvent(new CustomEvent("openContactModal"));
            });
        }
    });
</script>
